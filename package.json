{
  "name": "coixa-pi-sdk",
  "version": "0.1.4",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "scripts": {
    "prepare": "npm run build",
    "build": "tsup src/index.ts --dts",
    "dev": "tsup src/index.ts --dts --watch",
    "test": "tsup src/test/index.ts",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/node": "^24.3.1",
    "tsup": "^8.5.0",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "@scure/bip39": "^2.0.0",
    "@stellar/stellar-sdk": "^14.1.1",
    "micro-key-producer": "^0.8.1"
  }
}
